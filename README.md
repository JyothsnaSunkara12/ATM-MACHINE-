#ATM MACHINE 
This Python program simulates an ATM system. It verifies a user’s PIN and allows operations like checking balance, depositing, and withdrawing money. It uses a loop for repeated actions and handles invalid inputs using exception handling, ensuring smooth and secure execution until the user exits.
